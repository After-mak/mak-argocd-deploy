name: Direct Rollback Workflow

on:
  workflow_dispatch:

jobs:
  rollback:
    runs-on: ubuntu-latest
    steps:
      - name: Execute Rollback
        run: |
          echo "🚀 직전 커밋 버전으로 롤백 진행"
          