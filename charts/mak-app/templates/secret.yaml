apiVersion: v1
kind: Secret
metadata:
  name: redis-secret
  namespace: default # 인프라 담당자와 싱크 맞춰야할 네임스페이스
type: Opaque
stringData:
  # stringData를 사용하면 b64enc 없이 평문 그대로 써도 K8s가 자동 암호화
  REDIS_PASSWORD: {{ .Values.redis.password | quote }}