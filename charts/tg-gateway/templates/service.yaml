apiVersion: v1
kind: Service
metadata:
  name: tg-gateway-service
  labels:
    {{- include "tg-gateway.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "tg-gateway.selectorLabels" . | nindent 4 }}
